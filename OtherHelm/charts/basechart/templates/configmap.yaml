apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "application.configMapName" . }}
  labels:
    {{- include "application.labels" . | nindent 4 }}
data:
  # Environment variables from envDefaults and envOverrides
{{- include "application.configMapData" . | nindent 2 }}                  