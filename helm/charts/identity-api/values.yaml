# Identity API Configuration
# This chart deploys the eShop Identity API microservice using the basechart

base:
  # Application configuration
  application:
    name: &appName "identity-api"
  
  # Image configuration  
  image:
    registry: "ghcr.io/saadisfy/eshop"
    repository: "identity-api"
    tag: "1.0.0"
    pullPolicy: IfNotPresent
    pullSecret: "ghcr-secret"

  # Service configuration
  service:
    type: ClusterIP
    port: 80
    targetPort: 8080

  # Ingress configuration
  ingress:
    enabled: true
    className: "nginx"
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      kubernetes.io/ingress.class: nginx
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - host: &appHost "identity-api.saadisfy.me"
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: &appTlsSecret "identity-api-tls"
        hosts:
          - *appHost

  # PostgreSQL database configuration
  postgresql:
    enabled: true
    serviceName: ""  # If empty, defaults to: {{ .Release.Name }}-postgresql
    port: 5432
    connectionString: ""  # If empty, auto-generated from auth settings
    auth:
      database: "identitydb"
      username: "postgres"
      password: "CHANGEME"

  # Redis configuration (disabled for identity-api)
  redis:
    enabled: false

  # Shared services configuration (external EventBus)
  sharedServices:
    redis:
      enabled: false
    rabbitmq:
      enabled: true
      serviceName: ""  # If empty, defaults to external rabbitmq service
      port: 5672
      username: "user"
      password: "CHANGEME"
      subscriptionClientName: "Identity"

  # Application-specific configuration
  config:
    appSettings:
      Logging__LogLevel__Default: "Information"
      Logging__LogLevel__Microsoft.AspNetCore: "Warning"
      Identity__Issuer: "https://identity-api.saadisfy.me" #@cursor: what is this?
      # Note: EventBus settings are handled separately in the EventBus configuration section

  # Resources
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 250m
      memory: 256Mi

  # ServiceAccount
  serviceAccount:
    create: true
    annotations: {}
    name: ""
    automount: true

  # Health checks
  livenessProbe:
    enabled: true
    path: /health
    initialDelaySeconds: 30
    periodSeconds: 10

  readinessProbe:
    enabled: true
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 5